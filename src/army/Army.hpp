namespace army {

    struct Army {
        const int max_num_components;
        const int max_health_points;
        const int damage;
        const int damage_chance;
        int living_components;
        int current_health_points;
    };

}
